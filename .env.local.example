# El Ojo — Environment Variables
# Copy to .env.local and fill in values — NEVER commit .env.local

# ─── PHASE A ──────────────────────────────────────────────
# No API keys needed. All 6 world-state APIs are free/keyless.

# ─── Monument photos (optional — falls back to Wikipedia if unset) ────────
# Sign up free at https://unsplash.com/developers → "Your apps" → Demo key (50 req/hr)
# UNSPLASH_ACCESS_KEY=

# ─── PHASE B: AI Generation (Replicate) ───────────────────
# REPLICATE_API_TOKEN=

# ─── PHASE B+: NFT / IPFS ─────────────────────────────────
# PINATA_JWT=
# PINATA_GATEWAY_URL=https://your-gateway.mypinata.cloud

# ─── ANALYTICS (optional) ─────────────────────────────────
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=resonantmigration.xyz

# ─── RATE LIMITING (production) ───────────────────────────
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
